name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 0 */5 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping keepalive endpoint
        run: |
          curl -L https://vizsmith-ai.vercel.app/api/stay-awake
